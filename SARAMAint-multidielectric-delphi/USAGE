
USAGE:

$f90compiler = Name of your FORTRAN 90 compiler (ifort : Strongly Reccomended)

INSTALLATION:

./install.csh $f90compiler

RUN:

./CPint -inp  [PDB_filename]
./CPint -inp  1esv.pdb
./CPint -help (for help)

./refresh (To remove intermediate files for an interrupted or pre-terminated run of CPinterface.csh) 
./clean (to reset to the RAW / NASCENT form of the Package) 
If Cleaned, then ensure to run install prior to the next run of CPint (see USAGE above)
If only refreshed, no need to re-install the program


